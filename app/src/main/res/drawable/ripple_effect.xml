<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/rippleColor"> <!-- Цвет ripple-эффекта (полупрозрачный оттенок вашего основного цвета #d4a373) -->
    <item android:id="@android:id/mask">
        <shape>
            <corners android:radius="20dp" /> <!-- Соответствует cardCornerRadius -->
            <solid android:color="#FFFFFF" />
        </shape>
    </item>
</ripple>